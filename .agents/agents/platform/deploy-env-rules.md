# Deploy and Env Rules

- Distinguish build-time variables from runtime variables.
- Use secrets for server-only keys.
- Keep `keep_vars` policy explicit in deploy command/config.
- Validate D1 binding and worker entry before deployment.
